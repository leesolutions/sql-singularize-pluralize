Rails inflections based singularize and pluralize methods for TSQL.

How to use:
- select master.dbo.Pluralize('car')
  outputs: 'cars'

Config:
- download SqlRegEx.dll into c:\
- update database ref on top of install.sql file
- run install.sql on SQL Server 2005 instance
- test: select dbo.Pluralize('car')
- add regular expressions for singular, plural, irregular and uncountable into tsql_singularize_pluralize table. NOTE: use $1 for replace backreferences.


